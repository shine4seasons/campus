// Shared client-side middleware config for browser code in middleware/*
export const API_BASE = '/api';
export const API_URL = API_BASE;

export const STORAGE_KEYS = {
  TOKEN: 'campus_token',
  USER:  'campus_user',
};

export const ROUTES = {
  HOME:        '/',
  LOGIN:       '/login',
  LOGOUT:      '/logout',
  SELL:        '/sell',
  MY_PRODUCTS: '/my-products',
  DASHBOARD:   '/dashboard',
};
