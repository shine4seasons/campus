// API base path — cùng origin nên không cần full URL, không cần CORS
export const API_BASE = '/api';

// Routes trong app EJS (không còn /pages/xxx.html)
export const ROUTES = {
  HOME:        '/',
  LOGIN:       '/login',
  LOGOUT:      '/logout',
  SELL:        '/sell',
  MY_PRODUCTS: '/my-products',
  DASHBOARD:   '/dashboard',
};
